// This is code-a.js will be used to
// demonstrate how arglist works in Vim

function StuffAlpha(arg1, arg2 = 'foo') {
	var foo = 1
	var bar = 'a'
	var me = 'm'
	var foobar = foo + bar + me
	return foobar
}
